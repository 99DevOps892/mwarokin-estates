{
  "name": "mwarokin-estates",
  "version": "1.0.0",
  "private": true,
  "description": "Mwarokin Estates — premium property management platform by Syllogism Technology Africa (static app + Supabase backend)",
  "type": "module",
  "scripts": {
    "setup": "node auto-setup.js",
    "db:push": "supabase db push",
    "functions:serve": "supabase functions serve payments mpesa-stk-push mpesa-callback currency translations ai-lead-orchestrator --env-file .env",
    "functions:deploy": "supabase functions deploy payments mpesa-stk-push mpesa-callback currency translations ai-lead-orchestrator",
    "secrets:set": "supabase secrets set --env-file .env.supabase-secrets",
    "deploy:netlify": "netlify deploy --prod --dir=app",
    "verify": "node auto-setup.js --verify"
  },
  "devDependencies": {
    "supabase": "^2.0.0"
  }
}
